global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: minecraft
    static_configs:
      - targets: ["metrics-exporter:9150"]
        labels:
          server: zapeg
